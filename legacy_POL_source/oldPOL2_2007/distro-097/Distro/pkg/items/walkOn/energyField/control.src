/*
 * $Id: control.src 373 2006-06-17 18:27:33Z austinheilman $
 *
 */

use uo;
use os;

program control(field)
	while ( field )
		Sleep(1);
		if ( field.IsExpired() )
			DestroyItem(field);
			return 1;
		endif
	endwhile
endprogram
